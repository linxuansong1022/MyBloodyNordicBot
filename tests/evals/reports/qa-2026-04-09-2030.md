# QA Report — 2026-04-09 20:30

**Trigger**: auto-coder handoff (first time testing auto-coder → qa-tester协同)
**Scope**: `/clear` command implementation
**Changed files (QA-relevant)**:
- `src/cli-commands.ts` — new `/clear` entry + handler + context type extension
- `src/index.ts` — readline mode wiring (reassign closure)
- `src/tty-app.ts` — TUI mode wiring (in-place mutate closure)

**Changed files (QA-skipped)**: `DEV_SPEC.md` §9, `STUDY_LOG.md`, auto-coder skill refs, sync script (all doc/skill, no behavior)

**Model**: n/a (no LLM test ran in this QA round)

---

## Summary

| Status | Count |
|---|---|
| ✅ Pass | 1 |
| ❌ Fail | 0 |
| ⏭️ Skip | 2 |
| ⬜ Pending (user action) | 1 |
| **Total** | 4 |

---

## Test Results (strictly serial order)

### T1 — Type check
- Command: `npx tsc --noEmit; echo "exit=$?"`
- Status: ✅ **PASS**
- Evidence: `exit=0`, stdout empty, stderr empty
- Notes: clean baseline. Gate satisfied — proceed to downstream tests.

### T2 — Unit tests
- Command: `ls tests/evals/unit/ 2>&1; echo "exit=$?"`
- Status: ⏭️ **SKIPPED (Mode B)**
- Evidence: `ls: tests/evals/unit/: No such file or directory, exit=1`
- Reason: Unit test infrastructure not yet built (planned in §10.11 as "起步 1")
- Recommendation: `/clear`'s logic is a trivial state reset (`messages = [messages[0]]` / `length = 1`). Not worth a dedicated unit test — test code would exceed tested code. Defer to Pair Guard / dedup algorithm unit tests (§4.17 Tier 1) when that feature lands.

### T3 — Task eval
- Command: `ls tests/evals/tasks/ 2>&1; echo "exit=$?"`
- Status: ⏭️ **SKIPPED**
- Evidence: `ls: tests/evals/tasks/: No such file or directory, exit=1`
- Reason: Task eval infrastructure (§10.9 `runner.ts`) not yet built

### T4 — Manual smoke
- Status: ⬜ **PENDING (awaiting user)**
- Checklist: see "Manual Smoke Checklist" section below
- Blocking: the human user must run `npm run dev` and execute the checklist; AI cannot automate TUI interaction

---

## Manual Smoke Checklist

### SMOKE A: TUI mode (tty-app.ts path)

1. `cd /Users/songlinxuan/Desktop/MiniCode && npm run dev`
2. Wait for TUI to start
3. Type: `请记住我的名字叫 Alice，我的幸运数字是 42`
4. Wait for agent response
5. Ask: `我叫什么？幸运数字是多少？` — baseline: agent should say Alice, 42
6. Type: `/clear`
7. **Checkpoint 1 — UI feedback**:
   - ✓ See "Conversation cleared. System prompt retained."
   - ✗ No crash, no red error, input still accepts new messages
8. **Checkpoint 2 — memory actually cleared**:
   - Ask: `我叫什么？`
   - ✓ Agent should say "I don't know" / "you haven't told me"
   - ✗ If it still says "Alice" → messages weren't cleared properly
9. **Checkpoint 3 — system prompt preserved**:
   - Ask: `你是谁？你能做什么？`
   - ✓ Agent should introduce itself with tools available (read_file, grep_files, etc.)
   - ✗ If it says "I don't know who I am" or forgot tools → system prompt was wrongly deleted
10. `/exit` to quit

### SMOKE B: Adversarial checks

11. Restart: `npm run dev`
12. **Checkpoint 4 — /clear on empty conversation**:
    - Type `/clear` immediately (no prior messages)
    - ✓ Should still show "Conversation cleared. System prompt retained." — messages was already just [system], clearing is a no-op
    - ✗ Should not crash
13. **Checkpoint 5 — repeated /clear**:
    - Type `/clear` three times in a row
    - ✓ Same message each time, no state corruption
14. **Checkpoint 6 — tab completion**:
    - Type `/cle` then press Tab
    - ✓ Should complete to `/clear` (entry exists in SLASH_COMMANDS list)
    - ✗ If no completion → list not wired
15. **Checkpoint 7 — fuzzy input**:
    - Type `/clea` (typo, not Enter yet)
    - Press Enter
    - ✓ Should show "未识别命令。你是不是想输入：/clear"
    - ✗ Should NOT execute clear (strict equality match)
16. **Checkpoint 8 — mid-sentence /clear**:
    - Type `你好 /clear 帮我清一下`
    - Press Enter
    - ✓ This whole sentence should be sent to the agent as a prompt
    - ✗ Should NOT trigger the `/clear` command (only line-start `/clear` is a command)
17. `/exit`

### SMOKE C: readline mode (index.ts path) — optional

18. Test readline mode via pipe:
    ```
    printf '请记住 Alice\n/clear\n我叫什么\n/exit\n' | npm run dev
    ```
    Read the output to verify the same sequence works in non-TTY mode.

### Reporting back

Reply with:
```
SMOKE A: PASS | FAIL (which checkpoint?)
SMOKE B: PASS | FAIL (which checkpoint?)
SMOKE C: PASS | FAIL | SKIP
Notes: ...
```

---

## Report Validation (Iron Rule 6 self-check)

- [x] Every ✅ row has concrete output values (T1: `exit=0`)
- [x] Zero "should/would/expected" in ✅ rows (only in smoke checklist where they describe *user-observable* outcomes, which is allowed)
- [x] All ❌ rows have failure category with evidence (n/a — no failures)
- [x] Total row count = Pass + Fail + Skip + Pending (1 + 0 + 2 + 1 = 4) ✅
- [x] Scope properly filtered doc-only files out
- [x] Iron Rule 3 (zero cross-ref): no "verified earlier", each step independent
- [x] Iron Rule 5 (adversarial): smoke checklist includes 5 adversarial checks looking for bugs (empty state, repeated clear, tab completion, fuzzy input, mid-sentence)

---

## VERDICT

```
VERDICT: PASS (automated) — tsc clean, no failures in automatable checks
         PENDING manual smoke — user must run SMOKE A/B checklist
```

**qa-tester stops here. Does NOT commit. Human decides next step after smoke.**
